/**
 * src/domain/logging/hooks/index.ts
 *
 * Barrel export for logging hooks.
 * Use: import { useNetworkSync, useRealtimeSync } from '@/domain/logging';
 */

export { useNetworkSync } from './useNetworkSync';
export { useRealtimeSync } from './useRealtimeSync';
