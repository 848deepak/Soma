/**
 * src/domain/auth/index.ts
 *
 * Barrel export for auth domain.
 * Use: import { useAuth, useProfile } from '@/domain/auth';
 * Or: import { authAdapter, profileAdapter } from '@/domain/auth';
 */

export * from './hooks';
export * from './adapters';
