/**
 * src/domain/auth/adapters/index.ts
 *
 * Barrel export for auth adapters.
 * Use: import { authAdapter, profileAdapter } from '@/domain/auth/adapters';
 */

export { authAdapter } from './authAdapter';
export { profileAdapter } from './profileAdapter';
