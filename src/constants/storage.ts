/**
 * src/constants/storage.ts
 * Shared AsyncStorage key constants.
 * Import from here instead of redeclaring in each screen.
 */

/** Set to 'true' after user completes first launch (login or anonymous). */
export const HAS_LAUNCHED_KEY = '@soma_has_launched';
