/**
 * hooks/useRealtimeSync.ts (DEPRECATED)
 *
 * BACKWARD COMPATIBILITY SHIM - Remove after 2 weeks (target: 2026-04-21)
 *
 * New imports should use:
 *   import { useRealtimeSync } from '@/domain/logging';
 */

export * from '../src/domain/logging/hooks/useRealtimeSync';
